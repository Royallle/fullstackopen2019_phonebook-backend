{
  "name": "phonebook_backend",
  "version": "0.0.1",
  "description": "Backend part of the Phonebook exercise",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "watch": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf build && cd ../../fullstackopen2019/part2/phonebook && npm run build --prod && cp -r build ../../../fullstackopen2019_backend/phonebook_backend",
    "deploy": "git push heroku master",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push && npm run deploy",    
    "logs:prod": "heroku logs --tail"       
  },
  "repository": {
    "type": "git",
    "url": "fullstackopen2019_backend"
  },
  "author": "Royallle",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "morgan": "^1.9.1"
  },
  "devDependencies": {
    "nodemon": "^1.19.2"
  }
}
